# 2026-07-30 工作日志

## Mobile Tavern 技术评定（架构评审部分工作流）
- 由交付总监齐活林建立评审团队 `software-mobile-tavern-review`，派架构师高见远做静态技术审计（未改任何源码）。
- 综合评分 **85/100，等级 A-**。分维度：架构分层 85 / 完成度 88 / 代码质量 82 / 测试 90 / 文档 75 / 可运行性 85。
- 关键结论：
  - 分层铁律（ARCH-KERNEL/FLOW/COMPAT/CONFIG/PLATFORM）基本严守，且有 `architectureBoundaries.test.ts` 自动化守护；实测 `tsc --noEmit` 0 错误。
  - 用户感知的"写得一半的功能"并非代码桩：真实半成品只有 **WebXR AR（隐藏原型）** 与 **社区（代码完整、待生产/云端对齐）**，均属路线图/部署层面。
  - 主要隐患：① `src/services/` 与 `src/application/services` 分层漂移；② README 测试数(348)/目录结构与现状严重脱节，是"半成品误判"主源；③ `indexedDbMemoryStore.ts`(1058 行) 等超 1000 行文件、少量未登记豁免的 `any`。
- 改进建议 P0：更新 README 统计与目录概览、清理 `TableMemoryTab.tsx:270` 过时 TODO；将 `src/services/ar` 等 Adapter 迁至 `src/infrastructure/`。
- 应要求已导出正式评定报告落盘：`deliverables/software-company/mobile-tavern-delivery-2026-07-30.md`（含完整证据路径清单）。
- 按审计 P0 更新过时文档（工程师寇豆码，最小改动，IS_PASS=YES）：README.md 测试统计 348→611（附 Vitest95/Playwright4/CJS11 拆分）、目录结构简览重写为真实 `src/` 布局；`src/components/memory-drawer/TableMemoryTab.tsx:270` 过时 TODO 注释改为"创建/编辑能力已实现"。主理人已读改动确认。遗留观察：README 功能列表存在两个 `### 9.` 编号重复，待用户确认是否一并修正。
- 已修复 README 功能清单编号重复：测试套件 `### 9.`→`### 10.`、i18n `### 10.`→`### 11.`（工程师寇豆码，IS_PASS=YES，主理人读原文核对 9→10→11 连续无缺口）。
- 应要求做同类型项目定位/成熟度评定（市场调研部分工作流，产品经理许清楚，含 WebSearch 竞品调研）：定档为**中型独立项目，工程成熟度接近小型产品级（A-）**，但分发/社区仍 indie。对比 SillyTavern(31k★/200+贡献者)、SillyDroid/手机酒馆AI/MiniTavern(ST 套壳移动)、RisuAI、Moescape/猫箱(商业)。差异化：Tauri v2 真机原生+Rust 后端+零侵入数据驱动底座，在"移动 ST 兼容"中属空白路线。非 demo（74k 行+611 测试+真机 Tauri v2 打包），非商业产品级（无商店分发/单人主导/社区未成型）。

## expedition-inn 插件评审（架构评审部分工作流）
- 由交付总监齐活林建立评审团队 `software-expedition-review`，派架构师高见远对 `D:\projects\expedition-inn`（远征余响 expedition-echoes）做架构/可发布性评审。
- 用户自称"才刚刚开始"，实测为**后期原型 / 早期 MVP（非真正刚起步）**：已有完整可玩核心循环、纯函数 gameReducer、LLM 适配层、localStorage 存档与 v5→v12 迁移、107KB 手写 CSS、64KB App.tsx（30+ dispatch）。
- 综合评分 **62/100（作为可发布的 MT 插件标准）**。架构干净：domain 纯 TS（默认分支 exhaustiveness 校验 `const _exhaustive: never = action`）、UI 只 dispatch、LLM 仅返回一次性叙事文本且不可改状态、有关键测试。
- 唯一硬缺口：尚未打包为 `.mtplugin`——无 manifest、无打包脚本、存档未走 host bridge（`window.MobileTavernPlugin.llm.chat` 已接，但 storage 仍直连 localStorage）。
- P0：依赖链损坏——`npm test`/`npm run build` 当前因 vite8/vitest4 → rolldown 缺 `@rolldown/binding-win32-x64-msvc` 原生 binding 而失败；未打包适配 MT。
- P1：命名三处不一致（expedition-inn / expedition-echoes / 远征余响）；107KB 内联 CSS + 远程 Google Fonts @import（移动 WebView 需降级）；save key 与 config key 命名不统一（expedition-echoes.save.v12 vs expedition-inn:narrative-provider）。
- 结论：作为独立游戏原型质量不错，作为 MT 插件上架还差"打包+host 存档桥接+依赖修复"三件事。
