[default]
description = "Default permission set for the android-bridge plugin. The plugin exposes its capabilities through a @JavascriptInterface object injected into the WebView (window.AndroidThemeBridge) and does not register any Tauri IPC command, so no additional command-level permissions are required."
permissions = []
