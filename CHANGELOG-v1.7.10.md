# Mobile Tavern v1.7.10 更新日志

发布日期：2026-08-05

## 预设提示词开关回归

- 修复"自由编排启用后预设提示词控制什么都没有"的问题：此前「预设提示词配置」区域在自由编排模式下只剩一行跳转提示，Prompt 区块（子预设节点）开关无处可开。
- 现在自由编排模式下该区域直接列出当前编排的全部 Prompt 区块及启用/停用开关，并显示开启数/总数与 Token 估算；空编排给出明确提示；顺序、位置、内容等高级编辑仍由独立的「自由 Prompt 编排」分类负责。
- 新增 PromptsConfigSection 组件测试（区块列表渲染、开关切换、空编排提示、传统模式保留）。

## 测试稳定性

- MemoryTableDrawer 测试的异步等待预算从 3s 放宽到 8s，消除全量并行测试时 React.lazy 模块转换偶发超时导致的误判失败（该用例单独跑始终通过）。

## 验证

- lint、i18n、全量测试（87 套件 + vitest）与构建全部通过。
