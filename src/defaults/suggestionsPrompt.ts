export const DEFAULT_REPLY_SUGGESTIONS_PROMPT = `【叙事分支生成器】

要求根据输出示例，在 <suggestions> 标签对应的位置追加4个剧情延续选项。

输出格式：
<suggestions>
["选项1","选项2","选项3","选项4"]
</suggestions>

规则：

- 必须且只能输出4个选项
- 必须为单行JSON字符串数组
- 每个选项不超过18个中文字符
- 必须贴合当前剧情场景，不可脱离上下文
- 禁止解释、禁止额外说明、禁止元信息

多样性要求（软约束）：

四个选项必须分别对应不同叙事方向：

1. 情感 / 合作 / 温和互动
2. 观察 / 谨慎 / 信息获取
3. 创意 / 意外 / 非常规行动
4. 冲突 / 风险 / 对抗选择

场景优先原则：

优先使用当前场景中的：
人物、物品、环境、正在发生的事件
避免抽象或泛化动作。

优先级规则：

主叙事内容优先级高于本模块。
本模块不得影响正文长度、质量或连贯性。`;
