node_modules/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
scratch/
tmp/

# 临时 codegen / debug 脚本（一次性使用后不应入库）
temp_*

# Android signing keys
*.keystore
*.jks
*.p12

# Windows reserved device name (accidental redirect artifact in bash)
nul

# Playwright test artifacts
test-results/
playwright-report/

# Serverless cloud functions
serverless/

# Qoder IDE internal files
.qoder/

# Tauri 临时配置文件
src-tauri/temp_config.json

# Tauri 插件构建产物（android-bridge）
src-tauri/plugins/android-bridge/android/.gradle/
src-tauri/plugins/android-bridge/android/build/
src-tauri/plugins/android-bridge/android/app/build/

# Windows 便利脚本（非标准 npm 项目惯例）
npm.bat
npx.bat

# 测试用角色卡（不上传 GitHub）
test-cards/

# 全局忽略所有层级的 Gradle 缓存和编译生成物
**/.gradle/
**/build/*
# 保留项目构建配置（vite.config.ts 依赖）
!build/viteEnvironment.ts

# Rust 编译产物（cloud / shared workspace members）
target/
**/target/

# 注意：shared/bindings/ 下的 ts-rs 生成文件入 git，
# 以便前端开发者在未安装 Rust 工具链时也能获取类型定义。

# 测试诊断日志文件夹
tests/logs/

# JetBrains IDEA 本地配置
.idea/
