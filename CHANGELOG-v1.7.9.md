# Mobile Tavern v1.7.9 更新日志

发布日期：2026-08-05

## 预设回归修复

- 修复"自由编排开启后传统预设完全失效"的问题：提示词规划（自由编排）此前被设计为全局独立状态，预设包只保存传统 Prompt 字段，启用自由编排后预设的 CORE PROMPTS / PROMPT MODULES 子节点开关在运行时与 UI 中全部失效，切换预设也不会改变提示词规划。
- 规划归预设：`SavedPresetBundle` 新增 `composition` / `usePromptComposition`，导入 SillyTavern 预设时转换出的编排快照与开关随预设包保存；加载/删除预设时整体切换自由编排与编排快照；保存新预设携带当前编排状态；基础预设携带自己的编排快照。
- 旧预设包（无编排字段）加载时保持当前自由编排状态不变，向后兼容。

## 验证

- 使用真实 SillyTavern 预设文件（明月秋青 / 双人成行 / 夏瑾 天琴座）全链路验证：导入 0 错误，139/250/140 个提示词区块及各自启用/停用状态完整保留，开关任一区块后编译结果确实变化。
- 更新 usePresetBundles 测试（导入确认/取消、加载切换编排、旧包兼容、保存携带编排）；lint、全量测试与构建全部通过。
