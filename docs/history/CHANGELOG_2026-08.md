# 2026 年 8 月变更记录

## 2026-08-21

- 收紧自定义主题 CSS：按主题作用域隔离规则，阻止 Safe Area 变量覆盖及远程资源引用，并为主题运行时补齐激活态切换与回归测试。
- 设置页新增底部 Tab 显隐管理与稳定选择器；角色、设置保留为恢复入口，隐藏仅影响导航入口，不卸载页面或扩展注册。
- 新增独立本地界面资源库，支持图片、音频、视频的容量校验、预览与删除；资源字节不进入设置，通过宿主生成的安全 CSS 变量引用图片。
- 新增面向作者与 AI 的主题/界面扩展指南，统一说明主题包格式、CSS 能力、稳定选择器、Tab 显隐、本地媒体引用及全屏插件边界。

## 2026-08-20

- 优化角色首页的标题区、统计概览与布局切换层级；分支数改由 IndexedDB 轻量索引统计驱动并在数据变更后显式刷新，历史页冷启动直接使用持久化回合数与字数，不再显示错误零值或受会话分页影响。
- 完成历史会话存储收口：会话元数据与消息窗口分离，保留基于绝对轮次游标的消息分页；Prompt 改读权威历史，发送、删除、重生成与导入改为跨 Store 原子事务，并为变量和状态表增加版本化消息快照及旧数据降级。
- 完成全局一致性复审后的收口：消息提取改为合并 metadata，重发持久化统一附加状态快照，备份恢复保留全部富消息字段；历史编辑、角色级联删除与分支替换改为完整原子事务，后台抽取增加来源存活守卫，自动总结移动到消息提交之后，会话目录改用稳定复合游标。
- 删除废弃的 `AutoSummaryService`、单消息裸删除和全量消息同步入口；摘要并发、消息字段重启往返、下游摘要失效、派生记忆清理与导入旧尾部清理均增加真实 IndexedDB 回归测试。

## 2026-08-13

- 修复长会话旧内嵌切片遮蔽权威消息分页、摘要并发追加被合并吞掉及陈旧会话快照覆盖历史摘要；角色目录新增列表、书架、双列大卡布局并记忆选择。

## 2026-08-07

- 修复"仅思维链（正文为空）的助手消息无法重发"：重发目标改为基于全量消息定位，空正文消息可正常重发，Prompt 历史仍剔除占位符与空正文；重发/删除移除年表边界消息时同步修正 `lastSummarizedMessageId` 与年表卡片，防止总结服务重复总结；移除正文故事年表归档折叠，正文只由分页与虚拟列表控制，总结入口保留在聊天头部"故事年表"；新增边界一致性与重发回归测试，vitest 与 `npm test` 全部通过。

## 2026-08-05

- 发布 v1.7.10：自由编排模式下「预设提示词配置」区域直接列出全部 Prompt 区块及启用/停用开关，子预设节点开关回归可见可操作。

## 2026-08-05

- 发布 v1.7.9：修复"自由编排开启后传统预设完全失效"问题，提示词规划归预设——预设包携带编排快照与开关，切换预设整体切换提示词规划，子预设节点开关恢复可用。
- 使用真实 SillyTavern 预设文件全链路验证导入与区块开关，0 错误；旧预设包加载保持向后兼容。

## 2026-08-05

- 发布 v1.7.8：修复真机 LLM 流式响应中途断流（`error decoding response body`）后无法自动恢复的问题，未输出内容时自动重试一次，错误信息补充目标主机与已接收字节数。
- 修复仓库 `npm.bat`/`npx.bat` 包装脚本导致 Windows 下 `npm run X && npm run Y` 链式中断的环境问题，并启用仓库 Git Hooks 让推送自动执行质量门禁。

## 2026-08-04

- 完成小版本发布同步至 v1.7.7，统一更新应用版本标识、Tauri/Rust 配置、`public/version`、展示页与 `README.md`。
- 新增本次发版说明 `CHANGELOG-v1.7.7.md`，并同步技术文档的当前版本标记。

## 2026-08-03

- 修复 SillyTavern 聊天记录导入只保存会话元数据、重启后正文为空的问题；导入现在显式同步独立消息 Store。
- 统一备份升级为 v4，补齐自定义预设库、独立世界书、记忆词典、事件记忆与时态事实，明文导出统一脱敏所有应用内 API 凭证。
- 新增数据迁移应用服务与原子覆盖仓库；恢复前自动导出脱敏安全快照，正式恢复通过单个 IndexedDB 跨 Store 事务清理并替换用户数据，写入失败时整体回滚。
- 保持旧版备份兼容；旧格式缺少的数据会在确认恢复时明确提示，不静默伪装为完整备份。
- 角色卡本地导入与社区上传单文件上限由 10 MB 统一提升到 20 MB，并同步更新 Rust 服务校验、Nginx 部署示例和网络测试脚本边界。
- 重做角色卡社区的标题区、搜索区与双列卡片层级；列表和详情封面统一使用 `object-contain` 完整等比例展示，不再裁切角色图。
- 完善 SillyTavern Chat Completion 预设导入：优先解析 `character_id: 100001` 编排，保留 Prompt 标识、开关、注入元数据与正则深度字段，支持 `openai_max_tokens`；导入时可选择启用自由编排以完整恢复 Marker 语义，取消则保留原有编排。
- 新增 SillyTavern 预设兼容分析与导入报告，按数据形状识别完整兼容、核心兼容和仅识别预设，显示 Prompt、Marker、正则、TavernHelper 脚本体积、外部网络代码及数据库附着降级项。
- 新增类型化模型请求整形，支持相邻消息合并、system 聚合、角色包装、assistant prefill 与 stop sequences；默认关闭，SillyTavern 标准字段可随预设导入，最终 Prompt 预览与实际发送使用同一结果。
- 将叙事分支升级为受约束的结构化选择协议和原生按钮 UI，支持独立标签、实际输入文本与简短说明；旧字符串数组继续兼容，外部脚本、事件与工具字段一律丢弃。
- 新增会话级 Prompt 场景方案：可把当前区块开关保存为方案并在每个会话独立选择，发送和预览时作为基础编排的非破坏性覆盖层应用；方案缺失或引用旧区块时自动回退并输出诊断。
- 增强大型 Prompt 编排管理：支持搜索名称、正文、ID 与兼容来源，按角色、来源或注入位置分组，按编排顺序或 Token 成本排序，并可对当前可见区块批量启用、停用和删除；每条区块显示实时 trace 或原始模板 Token 估算。
- 为 SillyTavern 预设兼容建立主流形状回归矩阵：覆盖通用轻量旧/新版、数据库依赖型和重前端型，并守卫 Prompt 顺序、启用状态、正则计数、兼容分级与脚本隔离。
- 新增 SillyTavern 预设完整兼容测试类，贯通兼容分析、导入、Prompt 编译、数据库语义降级、前端脚本隔离、场景覆盖、Token 预算、请求整形和导出往返。
- 收口预设真实导入管线：外部字段清洗、`100001` 顺序、正则、请求整形和兼容分析统一进入无 IO 应用用例，设置 Hook 只保留文件交互、确认和持久化。
- 补齐正式预设导出：自由编排输出 `prompt_order`/In-Chat 字段，传统 Prompt 也生成 `100001` 顺序；请求整形与安全正则支持往返，并剥离保留的外部可执行脚本。
- 预设正则兼容数组、对象映射和顶层集合；无效项目不再静默丢弃，而是进入导入诊断并在确认及完成提示中展示。
- 新增真实预设文件验收命令，仅输出结构指标、兼容等级和耗时；可在不提交社区作者内容的前提下复测完整原文件。

## 记录约束

- 每项变更只写一行，建议不超过 180 个汉字。
- 只记录用户可见结果、关键边界变化与验证结论；完整实现细节写入 `TECHNICAL.md` 或专项文档。
- 每月新建一个 `CHANGELOG_YYYY-MM.md`，历史月份不属于默认阅读包。
