# v1.8.9 之后的分支审查

## 范围与结论

本次审查以 `v1.8.9` 为基线，原始待审查终点为 `04af263`。该范围包含 44 个提交、212 个文件，合计 15403 行新增与 3195 行删除。审查同时覆盖修复提交，最终范围为 `v1.8.9..HEAD`。

已逐项检查 Prompt 与预设、SillyTavern 世界书和 Regex、主题资源包、Agent Profile 与 Tool Plugin、iframe 兼容运行时、聊天命令、LLM 思考回填、设置与工作台、主导航、版本发布入口、配置和架构边界。确认的问题均已修复并按功能提交；截至记录完成时，没有已知但未处理的可操作审查意见。

## 修复结果

| 功能 | 问题与处理 | 提交 |
|---|---|---|
| Prompt 编辑同步 | 保留连续编辑时的动态数据源关联和 Marker 重命名语义 | `6391ef4` |
| 预设持久化 | 串行化异步保存，合并最新设置，避免连续切换和正文修改被旧操作覆盖 | `1292363` |
| 主题资源包 | 修复 JSON 字符串损坏、音频扩展名往返、导入失败残留和原生导出缺失 | `0dadd71` |
| 世界书运行时 | 将有效输出产生的时效状态随会话提交，修复递归、概率、预算与绝对消息位置语义 | `e0277c3` |
| Regex 兼容 | 清洗外部脚本，修复宏替换中的美元符号、命名捕获组和双模式执行 | `f543266` |
| Agent 预设 | 从公开默认数据补齐旧预设，防止缺省字段继承活动预设内容 | `c3ad2e2` |
| Tool SDK | 按 ZIP 中实际序列化的数据计算哈希，消除含 `undefined` 字段时的安装失败 | `b030af6` |
| Prompt 历史正则 | 在两条传统 Prompt 组装路径中传递历史深度 | `95a55d3` |
| iframe 资源清理 | 保留原生计时器参数语义，并保持卸载清理幂等 | `9360201` |
| 世界书导入与深度注入 | 保留高级字段、清洗边界输入并修复历史深度注入 | `03824fe` |
| 聊天命令 | 校正斜杠命令解析、分支与内置命令保护语义 | `46f33d4` |
| LLM 思考回填 | 以候选游标精确回填包装后的历史思考内容，避免重复子串错配 | `848f891` |
| Agent 工具挂载 | 冻结会话工具版本并修复挂载升级，避免运行中工具定义漂移 | `013a6d1` |
| 工作台 | 同步心相状态、容错损坏数据、按午夜刷新，并让存储和会话指标反映真实数据边界 | `e1d46ff` |
| 工作台分层 | 将心相物理存储移入基础设施层，通过应用用例访问 | `20df60c` |
| 设置界面 | 恢复分区按需加载，补全韩语和巴西葡萄牙语选择及插件分栏语义 | `fbff211`、`fba630b` |
| 主导航 | 补全保活页签的 ARIA 关系、减弱动效和移动端 12px 标签；E2E 定位限定到可见世界书面板 | `fac7ac5`、`dd40ebb` |
| 发布入口 | 恢复固定 APK 下载地址及版本脚本校验 | `4eb1960` |
| 变更卫生 | 清理审查范围中的行尾空白，保持 `git diff --check` 通过 | `10bd0e9` |

## 审查证据

- `npm run quality:push` 退出码为 0：86/86 项系统测试、160 个 Vitest 文件、1090 项测试、Web 生产构建和 Server 构建全部通过。
- `npm run test:e2e` 在桌面 Chromium 和 Pixel 5 项目中完成 30 个用例：28 个通过、2 个按既有条件跳过、0 个失败。两端的 50 轮长会话压力测试均记录 0 MB 堆内存增量。
- 定向世界书与 UI 性能回归为 8 个通过、2 个按既有条件跳过、0 个失败。
- `npm run check:i18n` 覆盖 8 种语言并通过；`npm run check:version` 确认 8 个版本来源均为 `1.9.0`。
- 架构边界系统测试通过；`src/kernel/` 没有本范围新增改动，变更未新增生产环境变量直读。
- `git diff --check v1.8.9..HEAD` 通过；审查新增代码没有显式 `any`。受战略门禁约束的 TypeScript 文件均未超过 1000 行。

## 验证边界

本次受控浏览器验证覆盖桌面和 Pixel 5 视口，没有在真实 Android 或 iOS 设备上执行原生安装包验证。生产构建仍报告既有的大 Chunk 警告，但不影响本次门禁结果；设置分区的按需加载已恢复，后续体积治理应作为独立性能工作处理。
