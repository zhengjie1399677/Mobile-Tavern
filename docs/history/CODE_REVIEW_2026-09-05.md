# v1.8.9 之后的分支审查

## 范围与完成条件

基线为 v1.8.9，原始待审查终点为 04af263；审查期间新增修复也需验证。目标为审查全部后续更改、修复确认问题、按功能提交并推送当前分支 codex/settings-ui-rebuild。以下为进行中记录，不能据此认定全范围审查或推送已完成。

## 已修复并提交

| 功能 | 问题与结果 | 提交 | 验证 |
|---|---|---|---|
| Prompt 编辑同步 | 连续编辑主提示词会丢失动态数据源关联；重命名 Marker 会清空模板。保留引用，仅明确编辑正文时替换内容 | 6391ef4 | 组件与 Hook 回归测试通过，提交检查通过 |
| 预设并发 | 异步保存覆盖其他设置、连续切换乱序。串行化预设操作，合并最新设置，检测保存期间的正文修改 | 1292363 | 并发与导入回归通过，提交检查通过 |
| 主题 ZIP | CSS 字符串替换破坏 JSON、音频扩展名不能往返、失败导入遗留资源、原生导出缺失。以值转换后序列化，补充回滚及原生保存 | 0dadd71 | ZIP 往返与失败回滚测试通过，提交检查通过 |

## 后续修复

世界书时效原先只写入临时上下文，无法随会话延续。修复为通用 Prompt 增量随有效输出提交，预览不写会话，并以绝对消息位置计算时效；同时修正 selectiveLogic 数字映射、递归排除/传播语义、延迟门槛、概率失败重抽和超预算条目的递归及状态污染。世界书解析、真实 Prompt 组装与发送相关回归已通过，提交为 e0277c3。

| 功能 | 问题与结果 | 提交 | 验证 |
|---|---|---|---|
| Regex | 宏值中的美元符号被当作替换指令、命名捕获组越界读到偏移量、双模式被双重排除。修正执行并在边界清洗外部脚本 | f543266 | 正则引擎与兼容入口回归通过 |
| Agent 预设与文件导入 | 旧预设缺省字段继承活动预设正文及正则。统一从公开默认数据补齐；默认数据移至 defaults，Hook 保持兼容导出 | c3ad2e2 | Profile、Bundle、启动及预设回归通过 |
| SDK | 可选 undefined 字段导致构建哈希与 ZIP 内 JSON 不同、安装失败。按实际序列化数据计算哈希 | b030af6 | 先复现 HASH_MISMATCH，修复后 SDK、包、验签及运行时回归通过 |
| Prompt Regex | 两种传统组装路径缺少历史深度。补齐参数并验证只替换指定深度 | 95a55d3 | 两种运行模式及 PromptService 回归通过 |
| iframe | 资源登记包装把字符串 interval 变成空操作。保留原生传参语义并测试幂等清理 | 9360201 | VM 执行与隔离/渲染回归通过 |

## 本轮精读与结论

- Agent Bundle 的导入、导出、Schema、Profile 持久化与一次性启动：已检查引用保留、缺失依赖诊断、公开字段和启动失败清理；预设继承问题已修复。版本冻结与 UI 调用链仍需复核。
- Tool Runtime、Host Capability、来源证明与验签、管理用例、包解析、存储差异和 SDK：已检查包哈希与签名绑定、固定信任来源、低风险命令限制、执行前授权和白名单宿主行为。SDK undefined 问题已修复；完整工具选择及 UI 链路仍需复核。
- iframe 清理、隔离脚本差异、srcdoc 与渲染差异：已检查会话键、卸载回收和脚本包装；interval 问题已修复。移动端实际渲染仍待受控浏览器回归。
- 2026-09-05 完整 npm test 已通过（86 项系统测试、157 个 Vitest 文件/1067 项测试）；这是 e0277c3 与 Regex 修复后的状态，不代表其后变更已跑完整门禁。

## 剩余审查区域

- 世界书导入导出与编辑优先级、Prompt 历史正则及深度注入。
- Regex 渲染与持久化调用链（引擎与外部输入已审查）。
- Agent/Profile 的工具版本冻结与 UI 启动调用链。
- Tool Plugin 目录、管理 UI、工具挂载版本；SDK 与运行时规范化哈希的额外边界值。
- iframe 隔离、资源清理与富文本渲染。
- 设置、聊天命令、主题交互、工作台与移动端界面。
- 其余配置、版本、文档和对应自动化变更。
- LLM 请求包装后的思考内容回填：子串匹配与候选游标需要进一步回归验证。
- 世界书和主题 ZIP 的归档与编辑边界值。
- 全范围完成复核、最终质量门禁、受控浏览器回归及推送结果核验。

## 当前完整门禁

9360201 状态的 npm run quality:push 已以退出码 0 完成：类型、改动文件 ESLint、i18n、系统测试、Vitest 和生产构建全部通过；测试为 86 项系统测试、159 个 Vitest 文件/1072 项测试。此门禁不代替上列尚未完成的审查，也不证明远端推送完成。

## 验证说明

局部测试通过只证明命中的回归场景。剩余区域及远端推送尚未完成，最终结果以更新后的本记录与 Git 远端状态为准。
