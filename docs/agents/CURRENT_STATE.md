# 当前状态

- 日期：2026-07-21。
- 主线：移动端底座解耦、聊天链路稳定性、Kernel 边界收敛。
- 最近完成：聊天发送、中止、重发与弱网恢复回归已补齐，断线部分回复标记可正确持久化；GitHub Android 正式签名改为缺失即失败；最新界面与故事年表修复已进入全新 APK 并安装真机。
- 当前风险：顶层功能使用 `React.lazy`，每个分包首次进入会显示短暂的全屏加载转圈；这不是 Rust DEBUG 包专属行为，需要在空闲预取与访问后保活之间确定体验策略。其余真机冒烟项等待用户手测。
- 产品决策：不建设独立的规则触发系统、数据派生计算引擎或移动端脚本代码编辑器；变量更新以角色卡 MVU、`mvu_zod`、`UpdateVariable` 和既有沙盒桥接为唯一兼容链路。
- 下一步：用户按 Android 冒烟矩阵手测首次切换、记忆中心、自由编排横屏、故事年表、文件导出、软键盘和双击返回；根据反馈决定顶层分包的预取或保活策略。iOS 不在当前范围内。
